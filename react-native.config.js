/**
 * Configuración del React Native CLI.
 * Declara la carpeta de fuentes para empaquetarlas en la app
 * (via `npx react-native-asset`).
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts'],
};
